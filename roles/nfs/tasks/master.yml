- name: Configure exports
  lineinfile:
    dest: /etc/exports
    state: present
    backup: yes
    line: "{{ item }}"
  with_items:
    - "/home {{ management_network }}(rw)"

- name: Enable some services for nfs server
  service: name={{ item }} state=started enabled=yes
  with_items:
    - nfs-server
    - rpcbind
